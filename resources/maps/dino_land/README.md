# Dino Land Map Pack

`dino_land_catalog.json` contains 10 prehistoric and dinosaur-themed maps normalized to the shared project map layout.

Original design JSON files are copied under `maps/json/` as source references. Stable PNG previews live under `res://assets/maps/dino_land/`.
